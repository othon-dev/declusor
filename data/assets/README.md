# Assets

This directory serves as a repository for static, non-executable resources required by operations. It is intended to store data that supports the execution of modules but does not contain logic itself.
